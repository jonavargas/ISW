@model IEnumerable<Data.BillDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Bill Detail</h2>

<div class="btnDiv">
    <p>
        @Html.ActionLink("New Bill Detail", "Create", null, new { @class = "btnCreateBD" })
    </p>
    <div class="labelCreateBD"><b>Add Category</b></div>
</div>

@using (Ajax.BeginForm(new AjaxOptions
{

    HttpMethod = "get",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "BillDetailList"

}))
{
    <div class="btn_search">
        <input type="text" name="Criterion" placeholder="Search Bill Detail" />
        <span class="glyphicon glyphicon-search , position"></span>
        <input type="submit" class="btn btn-success" value="Search" id="Search" />
    </div>

}
@Html.Partial("BillDetailParcial")

