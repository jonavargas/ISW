@model IEnumerable<Data.ProductSuppliers>

@{
    ViewBag.Title = "ProductSuppliers";
}

<h2>Product Suppliers</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Ajax.BeginForm(new AjaxOptions
{

    HttpMethod = "get",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "ProductSuppliersList"

}))
{
    <input type="text" name="Criterion" />
    <input type="submit" value="Search" id="Search" />

}
@Html.Partial("ProductSuppliersParcial")
