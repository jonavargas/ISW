@model Data.Brand

@{
    ViewBag.Title = "Details Brand";
}

<h2>Details Brand</h2>

<fieldset>
    <legend>Brand</legend>

    <table class="table table-bordered">
        <tr>
            <th>
                @Html.LabelForModel("Name")
            </th>
        </tr>
        <tr>
            <td>
                @Html.DisplayFor(model => model.Name)
            </td>
        </tr>
    </table>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
